{
  "name": "js-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev -r tsconfig-paths/register --inspect --transpile-only --env-file .env/.env.dev --ignore-watch node_modules --respawn src/index.ts",
    "test": "node --import tsx --env-file .env/.env.dev --test test/**",
    "type-check": "tsc --noEmit",
    "type-check:watch": "npm run type-check -- --watch"
  },
  "author": "Marcos Agostta (marcos.agostta@gmail.com)",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.7",
    "tsx": "^3.14.0",
    "typescript": "^4.9.5",
    "prettier": "3.0.0"
  }
}